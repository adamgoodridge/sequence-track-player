package net.adamgoodridge.sequencetrackplayer;

/*
public class GetFilesFromFolder {
    public static void main(String[] args) {
        String url = "https://radioplayer.adsmac.net/FRV/2019/AUDIOFILE_2019-05/AUDIOFILE_2019-05-02_Thursday/AUDIOFILE_MFB_2019-05-02_Thursday_23-15_TO_23-45";
        List<DataItem> items = getFiles(url);
        //         System.out.println(items.size());
    }
    private static List<DataItem> getFiles(String url) {
        String path = url.replace(ConstantText.DEFAULT_SERVER_URL,ConstantText.);
        File file = new File(path);
        if(file.listFiles() == null) {
            throw new NotFoundError("No nsw feeds cannot be found.");
        }
        return Arrays.stream(Objects.requireNonNull(file.listFiles())).map(DataItem::new).toList();
    }

}*/
